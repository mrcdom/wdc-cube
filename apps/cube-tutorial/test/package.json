{
  "name": "wdc-cube-tutorial-test",
  "version": "1.1.0",
  "description": "Exercises the tutorial's presentation layer with no view attached, alongside the React and Angular apps that render it",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "wdc-cube": "workspace:*",
    "wdc-cube-test": "workspace:*",
    "wdc-cube-tutorial-app": "workspace:*"
  },
  "devDependencies": {
    "reflect-metadata": "^0.2.2",
    "vitest": "^5.0.0"
  }
}
